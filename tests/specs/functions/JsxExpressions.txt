-- /test.tsx --
~~ {"tailwindFunctions": ["cn", "clsx"]} ~~
== sorts cn() inside JSX curly braces ==
<div className={cn("p-4 flex absolute")}>

[expect]
<div className={cn("absolute flex p-4")}>

== sorts clsx() inside JSX curly braces ==
<div className={clsx("p-4 flex absolute")}>

[expect]
<div className={clsx("absolute flex p-4")}>

== sorts function in array context ==
const cls = [cn("p-4 flex absolute")];

[expect]
const cls = [cn("absolute flex p-4")];

== sorts function in ternary context ==
const cls = isActive ? cn("p-4 flex absolute") : "";

[expect]
const cls = isActive ? cn("absolute flex p-4") : "";

== sorts function after logical operator ==
const cls = isActive && cn("p-4 flex absolute");

[expect]
const cls = isActive && cn("absolute flex p-4");

== sorts both className attr and function in same file ==
<div className="p-4 flex absolute">
  <span className={cn("p-4 flex absolute")}></span>
</div>

[expect]
<div className="absolute flex p-4">
  <span className={cn("absolute flex p-4")}></span>
</div>
